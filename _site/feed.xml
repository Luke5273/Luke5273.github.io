<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-02-14T20:54:11+05:30</updated><id>http://localhost:4000/feed.xml</id><title type="html">Azad’s Corner</title><subtitle>Control systems, robotics, and engineering notes</subtitle><entry><title type="html">The Equations that Control the World</title><link href="http://localhost:4000/2026/01/23/the-equations-that-control-the-world.html" rel="alternate" type="text/html" title="The Equations that Control the World" /><published>2026-01-23T00:00:00+05:30</published><updated>2026-01-23T00:00:00+05:30</updated><id>http://localhost:4000/2026/01/23/the-equations-that-control-the-world</id><content type="html" xml:base="http://localhost:4000/2026/01/23/the-equations-that-control-the-world.html"><![CDATA[<h2 id="introduction">Introduction</h2>
<p>Have you ever looked around and noticed how many machines – old or new – move with almost unsettling precision? Systems that track, stabilize, and correct themselves better than a human ever could? If you’ve built projects before, you might remember manually controlling each actuator. One input per motor, one knob per output. So how do real systems do it automatically? How do they decide how much to move, when to stop, and how fast to react?</p>

<p>Well, buckle in dear reader, for all the answers are in a small set of ideas and equations that quietly run cars, drones, factories, medical devices, and just about everything else that moves on purpose.</p>

<h1 id="feedback">Feedback</h1>

<p>Often when I talk to people about controls, they have this idea that you have to solve for something then invert that equation to figure out the input. <em>This may work</em>, but the work associated with it is immense and if there are any stochastic events, it would fall apart instantly.</p>

<p>Going with this approach at face value, let’s look at an example.</p>

<p>Imagine designing a shower system that can automatically set the water temperature. To get an exact set of equations for the temp, we would need to describe how the ambient temperature affects the water in the water tank, how pipes of variable lengths and thicknesses would lose heat, and various other processes. Not to mention random events like wear and tear, power cuts that affect pumps and water heaters, how many people are using the water in the tank, etc.</p>

<p>Could this be done? No, not to any real effect, since we have so many variables, most being <em>random, time-varying, and dependent on the installation</em>.</p>

<p>Lets take a step back and see how we do it. I want you to think about your own shower. It has one knob, pressing it in and out controls the water pressure and turning it controls the water temperature. For now, imagine the water pressure is either constant or inconsequential – just focus on the temperature.</p>

<p>If I ask you how to set the temperature, chances are you would not be able to do it on your first go. You’d put the water on, see if it’s too hot or cold, then adjust accordingly. Why can’t we make our system do the same thing?</p>

<p>Firstly, we need to shift our mindset from what the actual temperature is to <em>how different it is from what we want</em>. In formal terms, that’s the <strong>error</strong>. Depending on this error, we know if we need to make the water hotter or cooler.</p>

<p>But to see how different the temperature is, we need to <em>sense</em> it. When we’re using our own showers, we are the sensors. But for an automatic system, we need an external <strong>sensor</strong>.</p>

<p>Then, depending on how large the error is, we need to take some action. This is called the <strong>controller</strong>. In the shower example, we are the controller. We decide how much to change the ratio of hot and cold water by turning the knob.</p>

<p>This action is then applied back to the physical system itself – the water, heater, pipes, and tank. In control systems, this entire thing being acted upon is called the <strong>plant</strong>. The name comes from industrial automation, where the “plant” referred to the factory or process being controlled.</p>

<p>The process of continuously measuring what the system is doing, comparing it to what we want it to do, and using that difference to adjust the input is called <strong>feedback</strong>. Instead of trying to predict the world perfectly, feedback lets the system react to how the world actually behaves. It is quite possibly <em>the most important concept in control systems</em>, and nearly everything that follows builds directly on this idea.</p>

<h2 id="quantifying-system-performance">Quantifying system performance</h2>

<p>Before we talk about how to control things, we need to understand what we’re controlling. We need some way to quantify how a system behaves. For this, we look at another example.</p>

<p>Imagine a spring with a mass at the end. I know a lot of physics classes have this, showing how the mass oscilates between two points forever as a demo for potential energy and kinetic energy, but that’s always with a huge caviet: <em>We don’t consider friction</em>.</p>

<p>In actuality, what we expect is for the spring to oscilate, with each peak getting smaller and smaller. This is called <em>damping</em>, and there are actually four different variations of it.</p>

<p align="center">
  <img src="/assets/images/damping.png" alt="Damping diagram" />
</p>

<p>Okay, I know this graph seems really noisy but let’s walk through it step by step.</p>

<p>This is what’s known as a step response. It’s the output of the system to a <em>constant input</em>, which in this case is a constant <em>force</em>. Think of it like holding the mass steady and then suddenly letting it go. Gravity gives it a constant input.</p>

<p>The frictionless case I mentioned corresponds to the undamped plot. It’s slow to start, but quickly gets up to speed. Once it passes the midpoint, the spring will start pulling it back, decelerating the mass until it eventually moves up.</p>

<p>Then, with the</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Introduction Have you ever looked around and noticed how many machines – old or new – move with almost unsettling precision? Systems that track, stabilize, and correct themselves better than a human ever could? If you’ve built projects before, you might remember manually controlling each actuator. One input per motor, one knob per output. So how do real systems do it automatically? How do they decide how much to move, when to stop, and how fast to react?]]></summary></entry><entry><title type="html">From Hobby Projects to Real Embedded Firmware</title><link href="http://localhost:4000/2026/01/11/hobby-to-real-firmware.html" rel="alternate" type="text/html" title="From Hobby Projects to Real Embedded Firmware" /><published>2026-01-11T00:00:00+05:30</published><updated>2026-01-11T00:00:00+05:30</updated><id>http://localhost:4000/2026/01/11/hobby-to-real-firmware</id><content type="html" xml:base="http://localhost:4000/2026/01/11/hobby-to-real-firmware.html"><![CDATA[<h2 id="table-of-contents">Table of Contents</h2>

<ol>
  <li><a href="#Introduction">Introduction</a></li>
  <li><a href="#1-asserts-and-return-value-checking">Asserts and Return Value Checking</a></li>
  <li><a href="#2-watchdog-timers">Watchdog Timers</a></li>
  <li><a href="#3-state-machines">State Machines</a></li>
  <li><a href="#4-interrupts">Interrupts</a>
    <ul>
      <li><a href="#timer-interrupts">Timer Interrupts</a></li>
      <li><a href="#interrupt-based-serial">Interrupt-Based Serial</a></li>
      <li><a href="#isr-hygiene">ISR Hygiene</a></li>
    </ul>
  </li>
  <li><a href="#5-dma">DMA (Direct Memory Access)</a></li>
  <li><a href="#conclusion">Conclusion</a></li>
</ol>

<h2 id="introduction">Introduction</h2>

<p>I think it’s pretty common for kids interested in programming to be put into a robotics class at some point in their life. These often feature some kind of block-based programming a-la something like scratch. Eventually they end up writing arduino code, but then the progression kinda… stops. Everyone knows block-based isn’t the endgame solution, but why do people not think the same for beginner arduino style code?</p>

<p>I think I can try and answer this, as someone in college trying to get into the industry and having done an internship (which is impressive in this economy) and projects. Hopefully I can give industry vets a glimpse into what it’s like getting into the industry now – where the barrier for entry is so low – to people still stuck on the hobby side of things a few reasons why they should consider giving an honest effort to level up not only their coding game, but their systems level view on these projects.</p>

<p>Before we <em>jump right into it</em>, a bit about my experience. I am the electrical subsystem head at the AUV student project in my college. This involves not only a decent amount of coding myself, but also a lot of reviewing other people’s code. I also recentely completed an internship where we wrote a bootloader for the STM32 blue pill (F103C8T6) to enable over-the-air (OTA) updates using a GSM module. Before the internship I knew how to think in systems because of the AUV work, but the internship was my first exposure to real embedded code and it felt like my brain expanded three times at least. I want to share some common pitfalls and design decisions that someone who hasn’t seen real code can fall into.</p>

<h2 id="1-asserts-and-return-value-checking">1. Asserts and return value checking</h2>
<p>How many times have you seen something like:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">HAL_UART_Receive</span><span class="p">(</span><span class="o">&amp;</span><span class="n">uart1</span><span class="p">,</span> <span class="n">index</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">5000</span><span class="p">);</span>
<span class="n">index</span> <span class="o">=</span> <span class="n">index</span> <span class="o">-</span> <span class="sc">'0'</span><span class="p">;</span>
<span class="n">array</span><span class="p">[</span><span class="n">index</span><span class="p">]</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
</code></pre></div></div>
<p>Now, for the STM32 uninitiated, this is the code to read bytes from UART within a certain timeout. The arduino equivalent would be something like:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Serial</span><span class="p">.</span><span class="n">setTimeout</span><span class="p">(</span><span class="mi">5000</span><span class="p">);</span>
<span class="n">index</span> <span class="o">=</span> <span class="n">Serial</span><span class="p">.</span><span class="n">read</span><span class="p">();</span>
<span class="n">index</span> <span class="o">=</span> <span class="n">index</span> <span class="o">-</span> <span class="sc">'0'</span><span class="p">;</span>
<span class="n">array</span><span class="p">[</span><span class="n">index</span><span class="p">]</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
</code></pre></div></div>
<p>THIS IS DANGEROUS. You do not know if the data you were expecting is in the buffer. If the mcu didn’t receive anything and index wasn’t defaulted to something, we could get a segfault. The issue is that the UART read can fail silently by timing out. In this case, <code class="language-plaintext highlighter-rouge">HAL_UART_Receive(...)</code> can timeout, proceeding to the next line after those 5 seconds are over. A better way of doing this would be:</p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">auto</span> <span class="n">status</span> <span class="o">=</span> <span class="n">HAL_UART_Receive</span><span class="p">(</span><span class="o">&amp;</span><span class="n">uart1</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">index_byte</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">5000</span><span class="p">);</span> 

<span class="c1">// for arduino, check if the number of bytes received is correct via index == 1</span>
<span class="k">if</span> <span class="p">(</span><span class="n">status</span> <span class="o">==</span> <span class="n">HAL_OK</span><span class="p">)</span> 
<span class="p">{</span> 
    <span class="kt">int</span> <span class="n">index</span> <span class="o">=</span> <span class="n">index_byte</span> <span class="o">-</span> <span class="sc">'0'</span><span class="p">;</span>
    <span class="n">array</span><span class="p">[</span><span class="n">index</span><span class="p">]</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
<span class="p">}</span> 
<span class="k">else</span> 
<span class="p">{</span>
    <span class="c1">// handle UART read error or timeout</span>
<span class="p">}</span>
</code></pre></div></div>
<p>Now, this still isn’t perfect. You’d want to make sure <code class="language-plaintext highlighter-rouge">index_byte</code> is a valid index otherwise you’ll get another segfault.</p>

<p>This is called return value checking. It’s really quite important, since you’ll actually get to know if functions you call succeed or fail. The only unfortunate thing is that it makes your code a lot longer, which isn’t an issue per se, but it definitely can be tedious to write. A more common way of laying things out to try and mitigate this is as such:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span><span class="p">(</span><span class="n">HAL_UART_Receieve</span><span class="p">(</span><span class="o">&amp;</span><span class="n">uart1</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">index_byte</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">5000</span><span class="p">)</span> <span class="o">==</span> <span class="n">HAL_OK</span><span class="p">)</span> 
<span class="p">{</span>
    <span class="kt">int</span> <span class="n">index</span> <span class="o">=</span> <span class="n">index_byte</span> <span class="o">-</span> <span class="sc">'0'</span><span class="p">;</span>
    <span class="n">array</span><span class="p">[</span><span class="n">index</span><span class="p">];</span>
<span class="p">}</span>
<span class="k">else</span> 
<span class="p">{</span>
    <span class="c1">// handle UART read error or timeout</span>
<span class="p">}</span>
</code></pre></div></div>
<p>This is just a few extra characters to save a lifetime of hurt whilst debugging.</p>

<p>What we just did is an example of runtime error checking: your code actively tests whether the UART read succeeded and responds if it didn’t. This is great for reliability, but it comes at a cost: extra instructions, memory, and sometimes a bit of added latency. In systems where failures are rare and compute, memory, or timing budgets are tight, constantly checking every return value might be overkill. For those situations, it can be helpful to check assumptions only while testing. That’s where assert() comes in. An assert verifies that a condition you expect to be true actually is. If it isn’t, the program halts immediately, usually entering a debug trap where you go into a while(1) loop, which you can pretty easily see by halting the debugger.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">index</span> <span class="o">=</span> <span class="n">index_byte</span> <span class="o">-</span> <span class="sc">'0'</span><span class="p">;</span>
<span class="n">assert</span><span class="p">(</span><span class="n">index</span> <span class="o">&gt;=</span> <span class="mi">0</span> <span class="o">&amp;&amp;</span> <span class="n">index</span> <span class="o">&lt;</span> <span class="n">ARRAY_SIZE</span><span class="p">);</span>  <span class="c1">// sanity check only during testing</span>

<span class="n">array</span><span class="p">[</span><span class="n">index</span><span class="p">]</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
</code></pre></div></div>
<p>During development, this immediately catches programming mistakes (like sending the wrong indices) before they cause mysterious crashes in the field. In production, you can disable asserts (<code class="language-plaintext highlighter-rouge">#define NDEBUG</code>) so that they don’t consume CPU cycles or memory, leaving only your essential runtime checks. In safety-critical systems, asserts are sometimes left enabled but redirected to a controlled reset or fault handler instead of halting forever.</p>

<p>The rule of thumb is:</p>
<ul>
  <li>Runtime error checking for things that can realistically fail in normal operation (UART timeouts, sensor disconnection).</li>
  <li>Debug-time assertions for conditions that should never happen if the code is correct (Indexing outside an array).</li>
</ul>

<h2 id="2-watchdog-timers">2. Watchdog timers</h2>
<p>Remember those <code class="language-plaintext highlighter-rouge">while(1)</code> loops I mentioned earlier? Well what happens if we want to do something about it.</p>

<p>If something gets stuck, everyone’s first reaction is to turn it off and on. Embedded systems are no different. Watchdog timers are timers that you have to periodically reasure them that everything is fine. If they don’t get that reassurance, they hardware reset. It keeps track of a counter, which triggers the reset if it hits a user specified amount. The reassurance is setting the count back down to zero, effectively restarting the timer.</p>

<p>This isn’t useful only for asserts, it’s useful for any place where you risk going into an infinite loop. For example:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">volatile</span> <span class="n">bool</span> <span class="n">command_received</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">USART_IRQHandler</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> 
<span class="p">{</span>
    <span class="n">command_received</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
    <span class="p">...</span>
    <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">command_received</span><span class="p">)</span> 
    <span class="p">{</span>
        <span class="c1">// wait for command</span>
    <span class="p">}</span>
    <span class="p">...</span>
<span class="p">}</span>
</code></pre></div></div>
<p>Imagine a scenario where a machine waits for some kind of command, executes it, then halts till the next command. Another computer sends the command via UART. While designing a system, we know that the host computer will send a ping every 2 seconds. If we don’t get it, we know something is wrong. Maybe due to a cable issue, EMI, a UART peripheral glitch, or a bug elsewhere in the system. We don’t know, but going back to a known good state is out best bet.</p>

<p>Without a watchdog, someone would manually have to notice and powercycle the device. With a watchdog, we are able to automatically restart when we know (or at least strongly suspect) that we’ve reached a bad state.</p>

<p>Now you may be asking something along the lines of ‘what if the task I am trying to complete just takes a while? How does it know when to reset the counter?’ Well, dear reader, both of those are excellent questions and I am glad you’re paying attention.</p>

<p>The answer to both is that <em>you have to reset it</em>. You decide what has the possibly of being an infinite loop and where to put the watchdog resets. For example:</p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span><span class="p">(</span><span class="k">auto</span><span class="o">&amp;</span> <span class="n">pixel</span><span class="o">:</span> <span class="n">pixels</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">HAL_IWDG_Refresh</span><span class="p">(</span><span class="n">hiwdg</span><span class="p">);</span>
    <span class="n">processPixel</span><span class="p">(</span><span class="n">pixel</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>
<p>Now, the entire loop can take as long as it likes. The only way the watchdog can be triggered is in <code class="language-plaintext highlighter-rouge">processPixel(...)</code> takes too long. Remember, the watchdog timer is checking the heartbeat of your code. If that heartbeat stops, the program restarts.</p>

<h2 id="3-state-machines">3. State machines</h2>
<p>Have you ever written logic like this?</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wait_for_input()
do_something()
wait_for_next_input()
do_something_else()
...
</code></pre></div></div>
<p>This is all fine and dandy if you’re only ever doing one thing, but what if you have multiple systems working at the same time? If the input timings are non deterministic, this pattern falls apart completely. The solution to this issue is <em>state machines</em>.</p>

<p>Anyone that has done a digital design course is quaking in their boots right now. Yes, it turns out that state machines are useful outside of that class. Really, they’re useful in a lot of places because of how good they are at modelling behaviour. I’m not going to re-teach finite state machines here. If you’ve taken a digital design course, you already know the theory. If you haven’t, well spend like an hour watching a video on youtube. What matters in firmware is not what a state machine is, but how it lets you structure non-blocking, time-aware systems.</p>

<p>A state machine models behaviour as a set of discrete states and well defined transitions between them, driven by events. That sounds like a lot I know, but once you understand it, it’s really powerful when describing a surprising amount of things. Lets look at an example using a turnstile.</p>

<p>A turnstile is one of those machines at the metro which lets you pass if you have a valid ticket. Simplifying the working, we have two states:</p>

<ul>
  <li>Closed</li>
  <li>Open</li>
</ul>

<p>To go from one state to another we have these operations:</p>
<ul>
  <li>Closed to open -&gt; show a valid ticket</li>
  <li>Open to closed -&gt; inbuilt timer elapses</li>
</ul>

<p>We don’t necessarily have to indicate the inverses of these operations, but it is good practice.</p>
<ul>
  <li>Closed to closed -&gt; If an invalid ticket is presented</li>
  <li>Open to open -&gt; If the timer hasn’t elapsed</li>
</ul>

<p>It is easy to see the relation drawn out in a <em>state transition diagram</em> like this:</p>

<p align="center">
  <img src="/assets/images/turnstile_state_transition_light.svg" alt="State machine diagram" />
</p>

<p>The state machine is something that can very easily be translated to code. It becomes even easier if we pull it out into something like a <code class="language-plaintext highlighter-rouge">turnstile_control()</code> function.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">typedef</span> <span class="k">enum</span> <span class="p">{</span>
    <span class="n">TURNSTILE_CLOSED</span><span class="p">,</span>
    <span class="n">TURNSTILE_OPEN</span>
<span class="p">}</span> <span class="n">turnstile_state_t</span><span class="p">;</span>

<span class="k">static</span> <span class="n">turnstile_state_t</span> <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">;</span>
<span class="kt">void</span> <span class="nf">turnstile_control</span><span class="p">()</span>
<span class="p">{</span>
    <span class="k">switch</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="k">case</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">:</span>
            <span class="k">if</span><span class="p">(</span><span class="n">valid_ticket</span><span class="p">())</span>
            <span class="p">{</span>
                <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_OPEN</span><span class="p">;</span>
                <span class="n">open_gate</span><span class="p">();</span>
            <span class="p">}</span>
            <span class="k">break</span><span class="p">;</span>

        <span class="k">case</span> <span class="n">TURNSTILE_OPEN</span><span class="p">:</span>
            <span class="k">if</span><span class="p">(</span><span class="n">timer_elapsed</span><span class="p">())</span>
            <span class="p">{</span>
                <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">;</span>
                <span class="n">close_gate</span><span class="p">();</span>
            <span class="p">}</span>
            <span class="k">break</span><span class="p">;</span>
        
        <span class="nl">default:</span>
            <span class="n">assert</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span> <span class="c1">// state corruption</span>
            <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">;</span> <span class="c1">// recovery for release builds</span>
            <span class="n">close_gate</span><span class="p">();</span>
            <span class="k">break</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Over here it’s pretty easy to see how the state transitions map into the logic of the switch cases. What this allows us to do is advance the system by one small, deterministic step per tick, instead of waiting for an entire operation to finish. If we have multiple things going on, that lets us do a little bit of all of them.</p>

<p>The unfortunate thing however, is that this is still going to be troublesome code if we have the aforementioned multiple things going on. <code class="language-plaintext highlighter-rouge">close_gate()</code> and <code class="language-plaintext highlighter-rouge">open_gate()</code> may be functions that take a long time. This would mean that <code class="language-plaintext highlighter-rouge">turnstile_control()</code> can be a function that takes a long time. If we have multiple of these control loops going, like we would in this case, it makes the system far less reliable. Imagine we have turnstile control, but also need to keep track of the batteries used to power the turnstile. If the batteries discharge too much because we were stuck in the control loop, dead batteries. It would be an expensive mistake.</p>

<p>This gets to the heart of the state machine pattern. It enables <em>non-blocking</em> code.</p>

<p>Non-blocking code is when you write a system to be able to function without stopping everything else. Over here, calling <code class="language-plaintext highlighter-rouge">close/open_gate()</code> can be blocking. This makes the state machine blocking, going against the <em>exact pattern we were going after</em>.</p>

<p>A far better approach is to use the state machine to set flags that signal to the main loop to run some code. The key idea is to separate decision-making (state transitions) from work (hardware actions). This lets the state machine itself be nice and non-blocking without inherantly having to worry about the implementation. What that looks like is:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">volatile</span> <span class="n">bool</span> <span class="n">open_gate_flag</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
<span class="k">volatile</span> <span class="n">bool</span> <span class="n">close_gate_flag</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>

<span class="k">typedef</span> <span class="k">enum</span> <span class="p">{</span>
    <span class="n">TURNSTILE_CLOSED</span><span class="p">,</span>
    <span class="n">TURNSTILE_OPEN</span>
<span class="p">}</span> <span class="n">turnstile_state_t</span><span class="p">;</span>

<span class="k">static</span> <span class="n">turnstile_state_t</span> <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">;</span>
<span class="kt">void</span> <span class="nf">turnstile_control</span><span class="p">()</span>
<span class="p">{</span>
    <span class="k">switch</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="k">case</span> <span class="n">CLOSED</span><span class="p">:</span>
            <span class="k">if</span><span class="p">(</span><span class="n">valid_ticket</span><span class="p">()</span> <span class="o">&amp;&amp;</span> <span class="o">!</span><span class="n">open_gate_flag</span><span class="p">)</span>
            <span class="p">{</span>
                <span class="n">state</span> <span class="o">=</span> <span class="n">OPEN</span><span class="p">;</span>
                <span class="n">open_gate_flag</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="p">}</span>
            <span class="k">break</span><span class="p">;</span>

        <span class="k">case</span> <span class="n">OPEN</span><span class="p">:</span>
            <span class="k">if</span><span class="p">(</span><span class="n">timer_elapsed</span><span class="p">()</span> <span class="o">&amp;&amp;</span> <span class="o">!</span><span class="n">close_gate_flag</span><span class="p">)</span>
            <span class="p">{</span>
                <span class="n">state</span> <span class="o">=</span> <span class="n">CLOSED</span><span class="p">;</span>
                <span class="n">close_gate_flag</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="p">}</span>
            <span class="k">break</span><span class="p">;</span>
        
        <span class="nl">default:</span>
            <span class="n">assert</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span> <span class="c1">// state corruption</span>
            <span class="n">state</span> <span class="o">=</span> <span class="n">CLOSED</span><span class="p">;</span> <span class="c1">// recovery for release builds</span>
            <span class="n">close_gate_flag</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="k">break</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
    <span class="p">...</span>

    <span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="n">turnstile_control</span><span class="p">();</span>

        <span class="p">...</span>
        <span class="k">if</span><span class="p">(</span><span class="n">open_gate_flag</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">open_gate</span><span class="p">();</span>
            <span class="n">open_gate_flag</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="k">if</span><span class="p">(</span><span class="n">close_gate_flag</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">close_gate</span><span class="p">();</span>
            <span class="n">close_gate_flag</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="p">...</span>
<span class="p">}</span>
</code></pre></div></div>

<p>However now we run into another issue. Since <code class="language-plaintext highlighter-rouge">turnstile_control()</code> is still being called in the same loop as <code class="language-plaintext highlighter-rouge">open/close_gate()</code>, this doesn’t actually solve anything. We can still have <code class="language-plaintext highlighter-rouge">turnstile_control()</code> being called at erratic times.</p>

<p>What we’re missing is control over when <code class="language-plaintext highlighter-rouge">turnstile_control()</code> runs. As long as everything lives in the same <code class="language-plaintext highlighter-rouge">while(1)</code> loop, timing is implicit, fragile, and dependent on how long other code takes to execute. To make state machines truly reliable, we need a way to run them at predictable times – independent of the rest of the system.</p>

<p>NOTE: Flags should only be cleared <em>after the action is taken</em>. If done before, the state machine may run again and restart the action.</p>

<h2 id="4-interrupts">4. Interrupts</h2>
<p>Almost every arduino tutorial that I’ve seen – and trust me, I’ve seen a lot – teaches interrupts something like this:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">const</span> <span class="kt">int</span> <span class="n">buttonPin</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
<span class="k">volatile</span> <span class="kt">int</span> <span class="n">buttonState</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">buttonISR</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">buttonState</span> <span class="o">=</span> <span class="n">digitalRead</span><span class="p">(</span><span class="n">buttonPin</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">setup</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">pinMode</span><span class="p">(</span><span class="n">buttonPin</span><span class="p">,</span> <span class="n">INPUT</span><span class="p">);</span>

    <span class="n">attachInterrupt</span><span class="p">(</span>
        <span class="n">digitalPinToInterrupt</span><span class="p">(</span><span class="n">buttonPin</span><span class="p">),</span>
        <span class="n">buttonISR</span><span class="p">,</span>
        <span class="n">CHANGE</span>
    <span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">loop</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">buttonState</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// do something</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>If it’s a good tutorial, it may even explain what <code class="language-plaintext highlighter-rouge">volatile</code> means! They get some things right introducing ISRs and getting your head around the interrupt mental model. But the reason I don’t like this is because it really paints interrupts as this ‘fancy callbacks for a button’. In reality, hardware GPIO interrupts are only one of several interrupt sources available on a microcontroller. Some other interrupt sources are:</p>
<ul>
  <li>Timer interrupts</li>
  <li>ADC conversion complete interrupts</li>
  <li>UART RX/TX interrupts</li>
  <li>DMA interrupts</li>
  <li>SysTick (on ARM)</li>
  <li>Software-triggered interrupts</li>
</ul>

<p>Honestly, interrupts could take up a whole article by themselves. Even each of these sources could take up an article. In the interest of time though, I will give further reading as homework. Here, we will talk about timer interrupts amd  UART Rx/Tx, with DMA interrupts in their own section.</p>

<p>NOTE: Not all interrupts are equal, they have different priorities.</p>

<h3 id="timer-interrupts">Timer interrupts</h3>
<p>Timer interrupts have two modes. One-shot and periodic timers. The way that I think about them is like the mono-stable and a-stable modes of an oscillator or 555 timer. One-shot timers fire an interrupt a set time after they’re set. They’re very useful for tasks like mechanical inputs and setting delays, but what I really want to talk about right now are the periodic timers.</p>

<p>Periodic timers are the lifeblood of non-blocking code. They solve the earlier issue of not being able to control the timing of control loops. Determinism in loops like this is extremely important. A lot of algorithms require equally spaced points or samples.</p>

<p>In the turnstile example we took earlier, this is the final piece we need before it’s actually non-blocking.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="kt">void</span> <span class="nf">HAL_TIM_PeriodElapsedCallback</span><span class="p">(</span><span class="n">TIM_HandleTypeDef</span><span class="o">*</span> <span class="n">htim</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">htim</span><span class="o">-&gt;</span><span class="n">Instance</span> <span class="o">==</span> <span class="n">TIM2</span><span class="p">)</span>   <span class="c1">// example timer</span>
    <span class="p">{</span>
        <span class="n">turnstile_control</span><span class="p">();</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="k">volatile</span> <span class="n">bool</span> <span class="n">open_gate_flag</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
<span class="k">volatile</span> <span class="n">bool</span> <span class="n">close_gate_flag</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>

<span class="k">typedef</span> <span class="k">enum</span> <span class="p">{</span>
    <span class="n">TURNSTILE_CLOSED</span><span class="p">,</span>
    <span class="n">TURNSTILE_OPEN</span>
<span class="p">}</span> <span class="n">turnstile_state_t</span><span class="p">;</span>

<span class="k">static</span> <span class="n">turnstile_state_t</span> <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">;</span>
<span class="k">static</span> <span class="kt">uint32_t</span> <span class="n">open_timer_ms</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">turnstile_control</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">switch</span> <span class="p">(</span><span class="n">state</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="k">case</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">:</span>
            <span class="k">if</span> <span class="p">(</span><span class="n">valid_ticket</span><span class="p">())</span>
            <span class="p">{</span>
                <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_OPEN</span><span class="p">;</span>
                <span class="n">open_gate_flag</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
                <span class="n">open_timer_ms</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
            <span class="p">}</span>
            <span class="k">break</span><span class="p">;</span>

        <span class="k">case</span> <span class="n">TURNSTILE_OPEN</span><span class="p">:</span>
            <span class="n">open_timer_ms</span><span class="o">++</span><span class="p">;</span>

            <span class="k">if</span> <span class="p">(</span><span class="n">open_timer_ms</span> <span class="o">&gt;=</span> <span class="mi">2000</span><span class="p">)</span>  <span class="c1">// 2 seconds @ 1 kHz timer</span>
            <span class="p">{</span>
                <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">;</span>
                <span class="n">close_gate_flag</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="p">}</span>
            <span class="k">break</span><span class="p">;</span>

        <span class="nl">default:</span>
            <span class="n">assert</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>              <span class="c1">// state corruption</span>
            <span class="n">state</span> <span class="o">=</span> <span class="n">TURNSTILE_CLOSED</span><span class="p">;</span>
            <span class="n">close_gate_flag</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="k">break</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
    <span class="p">...</span>

    <span class="n">HAL_TIM_Base_Start_IT</span><span class="p">(</span><span class="o">&amp;</span><span class="n">htim2</span><span class="p">);</span>
    
    <span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="p">...</span>
        <span class="k">if</span><span class="p">(</span><span class="n">open_gate_flag</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">open_gate</span><span class="p">();</span>
            <span class="n">open_gate_flag</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="k">if</span><span class="p">(</span><span class="n">close_gate_flag</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">close_gate</span><span class="p">();</span>
            <span class="n">close_gate_flag</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="p">...</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Over here, Timer2 is set to trigger every 1ms. This allows us to bake timing into the state machine very easily, meaning that delays can now be non-blocking. Also, it lets the control parts of the code trigger even if the actual implementations are going on. This essentially makes a very rudimentary scheduler.</p>

<p>An important note, timer frequency is a design parameter, not a constant. You choose it based on how precise your system needs to be versus how much CPU time you can afford to spend in interrupts. We control two parameters, the prescaler and the period.</p>

<p>The prescaler is a scaler for the system/secondary clock that is driving the interrupt. It lets us divide the clock frequency by an arbitrary (integer) amount. The prescaler sets the <em>resolution</em> of each tick. Then the period is how many of those prescaled ticks we need to count before triggering the interrupt.</p>

<p>Let’s look at an example. Consider a chip capped at 60MHz. We’re first going to scale the clock down to 1MHz, then count to 1000. This has the effect of dividing by 1000, which brings 1MHz to 1KHz, or 1ms time period, which is what we wanted above.</p>

<p>It is important to remember that the interrupt gets called every 1ms though. If the logic inside the interrupt takes longer than 1ms, we run into timing jitter, where the time between sequential calls shifts because ISR execution time varies or exceeds the timer period, <em>which is exactly what we want to avoid</em>. This can be minimised by having good ‘ISR Hygiene’, which I will talk about in a minute.</p>

<h3 id="interrupt-based-serial">Interrupt based serial</h3>
<p>Imagine code code like this:</p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">String</span> <span class="n">command_str</span><span class="p">;</span>
<span class="kt">void</span> <span class="nf">loop</span><span class="p">()</span>
<span class="p">{</span>
    <span class="n">command_str</span> <span class="o">=</span> <span class="s">""</span><span class="p">;</span>
    <span class="k">while</span><span class="p">(</span><span class="n">Serial</span><span class="p">.</span><span class="n">available</span><span class="p">()</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="kt">char</span> <span class="n">c</span> <span class="o">=</span> <span class="n">Serial</span><span class="p">.</span><span class="n">read</span><span class="p">();</span>
        <span class="k">if</span><span class="p">(</span><span class="n">c</span> <span class="o">==</span> <span class="sc">'\n'</span><span class="p">)</span> 
            <span class="k">break</span><span class="p">;</span>
        <span class="k">else</span> 
            <span class="n">command_str</span> <span class="o">+=</span> <span class="n">c</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="k">auto</span> <span class="n">command</span> <span class="o">=</span> <span class="n">parseCommand</span><span class="p">(</span><span class="n">command_str</span><span class="p">);</span>
    
    <span class="k">switch</span><span class="p">(</span><span class="n">command</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="p">...</span>
    <span class="p">}</span>

    <span class="n">otherTasks</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>
<p>This looks very normal. In fact, it’s adapted from the example code in the arduino documentation for UART. What this code does is fill a command buffer with a command, parse it, and then continue with other tasks. On the surface, it’s completely fine, right? WRONG. There are a lot of pitfalls in it. Let’s walk through them.</p>

<p>First, exiting the loop requires us to encounter either a new line or for the serial buffer to empty. Neither of those things are guaranteed, so the execution of the other tasks can be completely starved if the uart buffer is being fed junk data. This is one of those bugs that won’t show up in the lab, but definitely will in the field where noise reigns supreme.</p>

<p>Second, this code assumes that when the serial buffer is empty, the entire command is read. This is also not guaranteed. Depending on how <code class="language-plaintext highlighter-rouge">parseCommand()</code> is written, this could very easily crash your system. This is a bug that you’ll encounter as the clock speed of your chip increases. On faster chips, the main loop can run multiple times between UART byte arrivals, making it more likely that <code class="language-plaintext highlighter-rouge">parseCommand()</code> sees incomplete data.</p>

<p>Third, if the other tasks take too long, the UART buffer on the mcu can overflow, causing you to lose data. With an arduino UNO and UART at 115200, the 64byte UART Rx buffer can fill up in about 6ms. That is not a lot of time. Unlike the second reason, this is a bug you’ll encounter on slower chips.</p>

<p>So, if this code has issues with faster chips and slower chips, whats the solution? Well, dear reader, considering we’re in the interrupts section and the interrupt based serial subsection, I’m sure you can make an educated guess.</p>

<p>The answer of course, is interrupts. Specifically interrupts that fire when the UART Rx buffer receives a byte (or multiple). What this allows you do to is to is to keep your own larger buffer depending on your application, which can control behaviours like overflowing, and let you signal to your main loop when data is ready to be processed. It also makes you keep the fact that you need to <em>detect</em> when commands are complete, rather than just kinda hoping and praying that you got a full command at the moment you decided to poll the serial buffer.</p>

<p>A better way of writing it is below.</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define CMD_BUF_SIZE 256
</span>
<span class="n">UART_HandleTypeDef</span> <span class="n">huart2</span><span class="p">;</span>

<span class="k">static</span> <span class="kt">uint8_t</span> <span class="n">rx_byte</span><span class="p">;</span>
<span class="k">static</span> <span class="kt">char</span> <span class="n">command_buf</span><span class="p">[</span><span class="n">CMD_BUF_SIZE</span><span class="p">];</span>
<span class="k">static</span> <span class="k">volatile</span> <span class="kt">uint16_t</span> <span class="n">cmd_index</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="k">static</span> <span class="k">volatile</span> <span class="n">bool</span> <span class="n">command_ready</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">HAL_UART_RxCpltCallback</span><span class="p">(</span><span class="n">UART_HandleTypeDef</span> <span class="o">*</span><span class="n">huart</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">huart</span><span class="o">-&gt;</span><span class="n">Instance</span> <span class="o">==</span> <span class="n">USART2</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="kt">char</span> <span class="n">c</span> <span class="o">=</span> <span class="p">(</span><span class="kt">char</span><span class="p">)</span><span class="n">rx_byte</span><span class="p">;</span>

        <span class="k">if</span> <span class="p">(</span><span class="n">c</span> <span class="o">==</span> <span class="sc">'\n'</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">command_buf</span><span class="p">[</span><span class="n">cmd_index</span><span class="p">]</span> <span class="o">=</span> <span class="sc">'\0'</span><span class="p">;</span>   <span class="c1">// null terminate</span>
            <span class="n">command_ready</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="n">cmd_index</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
        <span class="p">}</span>
        <span class="k">else</span>
        <span class="p">{</span>
            <span class="k">if</span> <span class="p">(</span><span class="n">cmd_index</span> <span class="o">&lt;</span> <span class="n">CMD_BUF_SIZE</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span>
            <span class="p">{</span>
                <span class="n">command_buf</span><span class="p">[</span><span class="n">cmd_index</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">c</span><span class="p">;</span>
            <span class="p">}</span>
            <span class="k">else</span>
            <span class="p">{</span>
                <span class="c1">// buffer overflow → reset safely</span>
                <span class="n">cmd_index</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
            <span class="p">}</span>
        <span class="p">}</span>

        <span class="c1">// Re-arm interrupt</span>
        <span class="n">HAL_UART_Receive_IT</span><span class="p">(</span><span class="o">&amp;</span><span class="n">huart2</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">rx_byte</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
    <span class="p">...</span>
    <span class="c1">// arm interrupt</span>
    <span class="n">HAL_UART_Receive_IT</span><span class="p">(</span><span class="o">&amp;</span><span class="n">huart2</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">rx_byte</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>

    <span class="k">while</span> <span class="p">(</span><span class="mi">1</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">command_ready</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">command_ready</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>

            <span class="n">command_t</span> <span class="n">command</span> <span class="o">=</span> <span class="n">parseCommand</span><span class="p">(</span><span class="n">command_buf</span><span class="p">);</span>

            <span class="k">switch</span> <span class="p">(</span><span class="n">command</span><span class="p">)</span>
            <span class="p">{</span>
                <span class="p">...</span>
            <span class="p">}</span>
        <span class="p">}</span>

        <span class="n">otherTasks</span><span class="p">();</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This is like the turnstile code, where we raise a flag and get the main loop to do all the heavy lifting; if we get another interrupt while we’re processing the last one, we may lose it. Futher, the advantanges of non-blocking code can be seen, with other tasks not being starved, and being more deterministic about timing. Also we have a 256 byte buffer instead of the measly 64 bytes we had earlier, which gives us about 20ms before the buffer fills up. We can make it even larger if we want though. While writing the OTA bootloader, When we were receiving the bytecode for the update, it was through a UART connection with the gsm module. The update was split into 1kb chunks and send along with some metadata like length and a checksum. So we made a 2048 byte buffer with no issue.</p>

<p>One limitation of this simple pattern is that it only stores one complete command at a time. If a second command arrives before the first one is processed, it will overwrite or be lost. In systems where command bursts are expected, this pattern is usually extended into a ring buffer or queue. However, I will also leave this as homework to the reader.</p>

<h3 id="isr-hygiene">ISR hygiene</h3>
<p>The last thing I want to say about interrupts – since this section has already become far longer than I anticipated – is about how to actually write them? I have mentioned jitter and hygiene, but what what exactly are they? Why are they important?</p>

<p>Interrupt Service Routines (ISRs) are special code. They do not behave like normal functions, and treating them as such is one of the fastest ways to create bugs that are impossible to reproduce. As a rule of thumb, an ISR should do the absolute minimum required to record that an event happened. When this rule of thumb is violated we get bad ISR hygiene, which is when code works, technically, but isn’t robust and is very prone to errors.</p>

<p>Here are some rules and why they are important:</p>

<hr />
<p><br /></p>
<h4 id="1-keep-isrs-short-and-deterministic">1. Keep ISRs short and deterministic</h4>
<p>An ISR should execute in a bounded and predictable amount of time, since while an ISR is running, other interrupts of equal or lower priority are blocked. Long ISRs increase interrupt latency and introduce jitter into time-critical systems.</p>

<p>Avoid parsing data, running control algorithms, calling slow HAL functions, anything with unbounded runtime. Almost all of these can be replaced with a flag that makes them run in the main loop.</p>

<h4 id="2-never-block-inside-an-isr">2. Never block inside an ISR</h4>
<p>ISRs must never wait for anything since an ISR that blocks can deadlock the entire system. The thing you are waiting for may itself depend on interrupts being serviced.</p>

<p>Avoid the use of delays, polling for conditions, waiting on flags, spinning in loops, or calling functions that might block internally. Instead, try using state machines for more complicated logic and delays.</p>

<h4 id="3-do-not-allocate-memory-in-an-isr">3. Do not allocate memory in an ISR</h4>
<p>Avoid dynamic memory entirely. Heap allocators are typically non-reentrant, non-deterministic, and may disable interrupts internally. Even if it “works once,” it will fail eventually.</p>

<p>Do not use malloc/new/String/containers, resize buffers, or rely on any heap-backed abstractions. Use simple stack based objects. If more complicated structures are required, use flags and implement logic in the main loop.</p>

<h4 id="4-share-data-with-the-main-loop-very-deliberatly">4. Share data with the main loop very deliberatly</h4>
<p>Be aware of compiler optimisations that can shoot you in the foot. Be liberal with your use of <code class="language-plaintext highlighter-rouge">volatile</code>. Use atomic features where possible, especially in multithreaded scenarios. (NOTE: <code class="language-plaintext highlighter-rouge">volatile</code> prevents compiler reordering, it does not make access atomic.)</p>

<p>Keep in mind that interrupts can be fired almost <em>anywhere</em>. In the middle of parsing a buffer, that buffer’s values can change. Never do major work on data shared with an interrupt, copy it into a local buffer.</p>

<hr />
<p><br />
In general, an ISR should record that something happened – not decide what to do about it.</p>

<p>I want to share a small anecdote specifically about point 4. The way we were checking to see if the GSM module was alive and working was to poll it for the IMEI number. Early in testing, the mcu was set at 8MHz. As soon as the IMEI was sent, the mcu received it properly. We then eventually increased it to 64MHz and suddenly it took at least six or seven attempts to read the data properly. Sniffing the UART showed that the data was being sent perfectly on either side, but looking at the RX buffer on the mcu showed that there were some strange null characters (‘\0’) that were messing things up.</p>

<p>The code looked like this (pseudocode):</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">UART_RxCallback</span><span class="p">()</span>
<span class="p">{</span>
    <span class="n">rxBuffer</span><span class="p">[</span><span class="n">rxLen</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">rxByte</span><span class="p">;</span>
    <span class="k">if</span><span class="p">(</span><span class="n">rxLen</span> <span class="o">&gt;=</span> <span class="n">RX_BUFFER_SIZE</span><span class="p">)</span>
        <span class="n">rxLen</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="n">rearmInterrupt</span><span class="p">(</span><span class="o">&amp;</span><span class="n">rxByte</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span> <span class="c1">// this is stm specific, but generally common</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">getIMEI</span><span class="p">()</span>
<span class="p">{</span>
    <span class="n">initialiseGsmModule</span><span class="p">();</span>
    <span class="n">wait</span><span class="p">(</span><span class="mi">30</span><span class="p">);</span> <span class="c1">//30s for gsm module to start up</span>
    
    <span class="k">while</span><span class="p">(</span><span class="n">attempts</span> <span class="o">&lt;</span> <span class="n">MAX_RETRIES</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="n">clearBuffer</span><span class="p">();</span>
        <span class="n">uartSend</span><span class="p">(</span><span class="n">IMEI_COMMAND</span><span class="p">);</span>

        <span class="n">wait</span><span class="p">(</span><span class="mi">2</span><span class="p">);</span> <span class="c1">//2s</span>
        <span class="k">while</span><span class="p">(</span><span class="n">noTimeout</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">last</span> <span class="o">=</span> <span class="n">rxLen</span><span class="p">;</span>
            <span class="n">rxBuffer</span><span class="p">[</span><span class="n">last</span><span class="p">]</span> <span class="o">=</span> <span class="sc">'\0'</span><span class="p">;</span>
            <span class="kt">char</span><span class="o">*</span> <span class="n">t</span> <span class="o">=</span> <span class="n">strstr</span><span class="p">(</span><span class="n">rxBuffer</span><span class="p">,</span> <span class="s">"IMEI"</span><span class="p">);</span>
            <span class="n">buf</span> <span class="o">=</span> <span class="n">imeiDecode</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>
            <span class="k">if</span><span class="p">(</span><span class="n">buf</span> <span class="o">==</span> <span class="n">GOOD_VALUE</span><span class="p">)</span>
            <span class="p">{</span>
                <span class="n">IMEI</span> <span class="o">=</span> <span class="n">buf</span><span class="p">;</span>
                <span class="k">break</span><span class="p">;</span>
            <span class="p">}</span>
        <span class="p">}</span>

        <span class="n">attempts</span><span class="o">++</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span> <span class="c1">//FAILED TO GET IMEI</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This is bad code. Just wanted to get that out of the way. Try and find the error before reading further.</p>

<p>The cause turned out to be a classic interrupt-related race condition. The RX interrupt was appending bytes to a shared buffer, while the main code was directly modifying that same buffer to temporarily insert a null terminator so that <code class="language-plaintext highlighter-rouge">strstr()</code> could operate on it. The code assumed this was safe because the null would be overwritten by the next received byte anyway. However, every so often, the UART interrupt would fire between reading the current buffer length and writing the null terminator. When that happened, real incoming data was overwritten instead.</p>

<p>At 8 MHz, this race window was small enough that we almost never hit it. At 64 MHz, the MCU was looping much faster relative to the UART byte arrival rate, dramatically increasing the chances of interrupting ourselves at exactly the wrong moment.</p>

<p>Making the code copy not only the length of the buffer, but of the buffer itself solved this problem instantly.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">getIMEI</span><span class="p">()</span>
<span class="p">{</span>
    <span class="n">initialiseGsmModule</span><span class="p">();</span>
    <span class="n">wait</span><span class="p">(</span><span class="mi">30</span><span class="p">);</span> <span class="c1">//30s for gsm module to start up</span>
    
    <span class="k">while</span><span class="p">(</span><span class="n">attempts</span> <span class="o">&lt;</span> <span class="n">MAX_RETRIES</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="n">clearBuffer</span><span class="p">();</span>
        <span class="n">uartSend</span><span class="p">(</span><span class="n">IMEI_COMMAND</span><span class="p">);</span>

        <span class="n">wait</span><span class="p">(</span><span class="mi">2</span><span class="p">);</span> <span class="c1">//2s</span>
        <span class="k">while</span><span class="p">(</span><span class="n">noTimeout</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">last</span> <span class="o">=</span> <span class="n">rxLen</span><span class="p">;</span>
            <span class="n">memcpy</span><span class="p">(</span><span class="n">rxSlice</span><span class="p">,</span> <span class="n">rxBuffer</span><span class="p">,</span> <span class="n">last</span><span class="p">);</span>
            <span class="n">rxSlice</span><span class="p">[</span><span class="n">last</span><span class="p">]</span> <span class="o">=</span> <span class="sc">'\0'</span><span class="p">;</span>
            <span class="kt">char</span><span class="o">*</span> <span class="n">t</span> <span class="o">=</span> <span class="n">strstr</span><span class="p">(</span><span class="n">rxSlice</span><span class="p">,</span> <span class="s">"IMEI"</span><span class="p">);</span>
            <span class="n">buf</span> <span class="o">=</span> <span class="n">imeiDecode</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>
            <span class="k">if</span><span class="p">(</span><span class="n">buf</span> <span class="o">==</span> <span class="n">GOOD_VALUE</span><span class="p">)</span>
            <span class="p">{</span>
                <span class="n">IMEI</span> <span class="o">=</span> <span class="n">buf</span><span class="p">;</span>
                <span class="k">break</span><span class="p">;</span>
            <span class="p">}</span>
        <span class="p">}</span>

        <span class="n">attempts</span><span class="o">++</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="k">while</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span> <span class="c1">//FAILED TO GET IMEI</span>
<span class="p">}</span>
</code></pre></div></div>
<p>This bug, which I spent far far too long trying to find made sure I never forgot about point 4.</p>

<h2 id="5-dma">5. DMA</h2>
<p>Full honesty, I have not used DMA. I have seen it be used in the firmware at my internship, and we plan on using it with our ADCs since <em>we kinda have to</em>. But I’ll do my best to explain what they are and why/where you’ll use them.</p>

<p>So far, every example we’ve looked at assumes the CPU is involved whenever data moves: a UART byte arrives, an interrupt fires, the CPU copies the byte into a buffer, and then goes back to whatever it was doing. This works fine at low data rates, but it doesn’t scale. If you are receiving hundreds of kilobytes per second, or streaming ADC samples continuously, the CPU quickly becomes a glorified data copying machine.</p>

<p>DMA exists to solve this exact problem. Direct Memory Access allows peripherals to transfer data directly to or from memory without CPU intervention for each byte. Once configured, the DMA controller autonomously moves blocks of data while the CPU continues executing other code or sleeps entirely.</p>

<p>DMA does not replace interrupts, it changes what interrupts are used for. Instead of firing an interrupt for every byte, the system typically raises an interrupt only when a buffer is full, half full, or when a transfer completes. This drastically reduces interrupt rate and jitter. A UART running at 1 Mbaud generates one million bit events per second. With interrupts per byte, that’s 100,000 interrupts per second. With DMA, that might become one interrupt every 1 ms or even less. This means that interrupts (and by extension the CPU) will be used to figure out when to do something, rather than actually doing the thing.</p>

<p>This article won’t walk through DMA setup or configuration. DMA is highly mcu specific, tightly coupled to bus architecture, and poorly abstracted across vendors. At this stage, what matters is recognizing when DMA is the right tool, not memorizing how to configure it, which is whenever data rates are high, timing or power matters, or the CPU just has better things to do than move memory around.</p>

<h2 id="conclusion">Conclusion</h2>
<p>At the end of the day, all of this really comes down to being resource aware. On a microcontroller, time, memory, and determinism are finite resources, and every design choice spends some amount of them. Polling loops burn CPU time, interrupts introduce latency and shared state, buffers consume RAM, and higher clock speeds don’t make bugs disappear, they often make them easier to trigger. Good embedded code isn’t about being clever, it’s about knowing what you’re paying for and deciding whether that cost is acceptable for your system.</p>

<p>This is also where the difference between Arduino-style development and lower-level firmware starts to matter. Arduino hardware is perfectly capable (if expensive), but the HAL makes a number of convenience-driven choices on your behalf: dynamic memory allocation, implicit polling, hidden buffers, and opaque timing behavior. None of these are wrong, and for many projects they’re exactly what you want. The problem only appears when those abstractions leak and you don’t know what’s happening underneath. At some point, especially as systems grow more complex or more constrained, you need to understand the costs those abstractions hide.</p>

<p>This is why Arduino has a mixed reputation among engineers. It’s not because the platform is bad, but because it makes it easy to chain black boxes together without ever needing to reason about timing, memory, or concurrency. Writing firmware without those abstractions can feel intimidating at first, because there’s simply more you need to keep in mind. At the same time, that extra explicitness often feels better once you’re comfortable with it. Having to think about GPIO banks in something like <code class="language-plaintext highlighter-rouge">HAL_GPIO_WritePin()</code> makes it harder to forget what the hardware is actually doing.</p>

<p>Most of the concepts discussed here can be implemented in Arduino code as well, but some of them are awkward or unintuitive. Timer interrupts are a common example, since they’re already tied up in core functionality like PWM and servo libraries. Regardless of the framework you use, the important takeaway is to think consciously about the decisions you’re making. Embedded systems are long-lived, tightly constrained, and often maintained by someone else later. Being deliberate about resource usage and structure is one of the best favors you can do for future you or the poor soul who has to read your code next.</p>

<p>Finally, it’s worth briefly mentioning RTOSes. An RTOS doesn’t magically solve the problems discussed here, it just gives you more structured tools to manage them. Tasks, queues, and semaphores are all ways of making resource usage, timing, and concurrency explicit rather than accidental. Used well, an RTOS can improve clarity and determinism; used poorly, it can hide the same problems behind another layer of abstraction. The same rules still apply: understand what the system is doing, know the costs, and choose the right level of complexity for the job.</p>

<p>Anyway, thank you for reading this far. Hope you’ve learnt something useful, or at least have an idea of real embedded code looks like.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Table of Contents]]></summary></entry></feed>